# simple-3d-scene-viewer
An interactive scene viewer written in Rust. Users can load a 3D scene and navigate it with local illumination, record their camera movement, and then render the recorded path offline using path tracing (global illumination).
